<IfModule mod_rewrite.c>
    RewriteEngine On
    # Redirect all requests to public/ folder if DocumentRoot is backend_saas
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
